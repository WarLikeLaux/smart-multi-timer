# ПОЛИТИКА РАЗРАБОТКИ v1 — **KISS > YAGNI > DRY**

*Контекст: **Python 3.x**, **Tkinter + ttkthemes**, **PyGame**, **Telethon**, **PyInstaller***
*AI Tools: **Claude Code**, **GitHub Copilot**, **Cursor***

---

## ⚠️ НЕПРЕЛОЖНЫЕ ТРЕБОВАНИЯ

* **ПРИОРИТЕТ:** **KISS > YAGNI > DRY > SOLID** — ВСЕГДА.
* **GUARD-CLAUSES ВСЕГДА** (РАННИЕ ВЫХОДЫ, ПРЕДУСЛОВИЯ).
* **ВЛОЖЕННОСТЬ ≤ 2 УРОВНЯ**.
* **ДЛИНА ФУНКЦИИ: ЦЕЛЬ ≤ 25 СТРОК, ПОТОЛОК ≤ 50**.
* **КОММЕНТАРИИ МИНИМАЛЬНЫ**, ТОЛЬКО КОГДА ДОБАВЛЯЮТ ЦЕННОСТЬ *(см. секцию ДОКУМЕНТАЦИЯ)*.
* **СЛОЖНОСТЬ ПРЯЧЕМ ВНУТРИ**, ПУБЛИЧНЫЕ API — ПЛОСКИЕ И СТАБИЛЬНЫЕ.
* **EXPLICIT > IMPLICIT** — ЯВНЫЕ ТИПЫ (TYPE HINTS), ЯВНЫЕ ЗАВИСИМОСТИ, ЯВНЫЕ КОНТРАКТЫ.
* **ФАЙЛЫ ≤ 500 СТРОК** — превышение = обязательный рефакторинг (см. 📁 ОРГАНИЗАЦИЯ ФАЙЛОВ).
* **РУССКИЙ ЯЗЫК ДЛЯ UI** — весь пользовательский интерфейс ТОЛЬКО на русском языке.
* **РУССКИЙ ЯЗЫК ДЛЯ ДОКУМЕНТАЦИИ** — README, docstrings, комментарии на русском.
* **АНГЛИЙСКИЙ ДЛЯ КОДА** — имена переменных, функций, классов на английском.

---

## 🧭 ИЕРАРХИЯ ИНСТРУКЦИЙ (ДОЛЖНА СОБЛЮДАТЬСЯ БЕЗУСЛОВНО)

1. **СИСТЕМА/ПОЛИТИКА** → 2) **КОМАНДА/РАЗРАБОТЧИК** → 3) **БРИФ ПОЛЬЗОВАТЕЛЯ** → 4) **ДАЛЕЕ КОНТЕКСТ**.
   **ЕСЛИ КОНФЛИКТ:** ВЫПОЛНЯТЬ БОЛЕЕ ВЫСОКИЙ УРОВЕНЬ. **ЕСЛИ ДВОЙСТВЕННОСТЬ:** СПРОСИТЬ УТОЧНЕНИЕ ИЛИ ВЫБРАТЬ САМУЮ РИСКО-НЕЙТРАЛЬНУЮ ИНТЕРПРЕТАЦИЮ.

---

## 📏 ДЛИНА / ВЛОЖЕННОСТЬ / СЛОЖНОСТЬ

* **ФУНКЦИИ:** **≤ 25** СТРОК (ЦЕЛЬ), **≤ 50** (ЖЁСТКИЙ ПОТОЛОК).
* **ВЛОЖЕННОСТЬ:** **≤ 2**.
* **ЦИКЛОМАТИЧЕСКАЯ СЛОЖНОСТЬ:** **≤ 10**. **КОГНИТИВНАЯ:** **≤ 15**.
* **ПРИ ПРЕВЫШЕНИИ:** ДЕКОМПОЗИЦИЯ НА МАЛЫЕ ШАГИ/ХЕЛПЕРЫ.
* **ОДНА ФУНКЦИЯ = ОДИН УРОВЕНЬ АБСТРАКЦИИ** — не смешивать high-level логику и low-level детали.

---

## 🔁 ПОТОК УПРАВЛЕНИЯ

* **GUARD-CLAUSES ОБЯЗАТЕЛЬНЫ**; **IF/ELSE** — ТОЛЬКО ДЛЯ ВЗАИМНО ИСКЛЮЧАЮЩИХ ВЕТОК.
* **> 2 ВЕТОК ПОВЕДЕНИЯ:** ИСПОЛЬЗОВАТЬ ТАБЛИЦУ ДИСПЕТЧЕРИЗАЦИИ/СТРАТЕГИЮ/ПОЛИМОРФИЗМ.
* **ИЗБЕГАТЬ ОТРИЦАНИЙ И ВЛОЖЕННЫХ "ИНАЧЕ"**; ПРЕДПОЧИТАТЬ РАННИЕ ВОЗВРАТЫ.
* **ЛИНЕЙНЫЙ КОД ПРЕДПОЧТИТЕЛЬНЕЕ РАЗВЕТВЛЁННОГО** — guard clauses делают код AI-readable.

---

## 📁 ОРГАНИЗАЦИЯ ФАЙЛОВ

* **ЦЕЛЬ ≤ 200 СТРОК/ФАЙЛ**, **ДОПУСК ≤ 350**, **ПОТОЛОК 500** — превышение = обязательный рефакторинг/разбиение.
* **ИСКЛЮЧЕНИЕ:** крупные enum и legacy-блоки (помечай `# TODO: refactor`, планируй вынос).
* **ОДИН КЛАСС = ОДИН ФАЙЛ**; допустимо только компактное DTO + Exception (<30 строк) если связаны логикой.
* **НЕ СМЕШИВАТЬ** helper и model (разные ответственности → разные файлы).
* **ВЫНОСИ КЛАСС**, если выполняются: Single Responsibility, High Cohesion, AI readability (целиком видно без скролла), четкая граница интерфейса.
* **РАЗБИВАЯ СЕМЕЙСТВО**, держи явные зависимости через конструктор, консистентные имена (`*Tab`, `*Service`, `*Storage`, `*Notification`), фиксируй какие файлы за что отвечают.

---

## 🏷️ ИМЕНА И ПУБЛИЧНЫЕ API

* **КОМАНДЫ ≠ ЗАПРОСЫ** (НЕ СМЕШИВАТЬ ПОБОЧНЫЕ ЭФФЕКТЫ И ЧТЕНИЕ).
* ИМЕНА: **ГЛАГОЛ + ОБЪЕКТ + КОНТЕКСТ**; СТАБИЛЬНЫЙ ПОРЯДОК СЛОВ.
* **ЗАПРЕТ БУЛЕВЫХ ФЛАГОВ-ПЕРЕКЛЮЧАТЕЛЕЙ ПОВЕДЕНИЯ**; АЛЬТЕРНАТИВНЫЕ СЦЕНАРИИ — ОТДЕЛЬНЫЕ МЕТОДЫ.
* **ПАРАМЕТРОВ ≤ 4**; СЛОЖНЫЕ СГРУППИРОВАТЬ В DATACLASS/NAMEDTUPLE.
* **DOMAIN-SPECIFIC TERMINOLOGY** — используй терминологию предметной области, не generic имена (Manager, Helper, Utility, Handler, process, do_stuff).
* **КОНСИСТЕНТНОСТЬ СУФФИКСОВ:** `*Tab`, `*Window`, `*Storage`, `*Notification`, `*Reminder`, `*Player`.

---

## 🎯 ТИПИЗАЦИЯ И EXPLICIT CODE (КРИТИЧНО ДЛЯ AI)

### Python 3.8+ Type Hints
* **COMPREHENSIVE TYPE HINTS** — параметры, return types, переменные класса — всё типизировано.
* **DATACLASSES** для структур данных — используй где применимо:
  ```python
  from dataclasses import dataclass

  @dataclass
  class TimerConfig:
      hours: int
      minutes: int
      seconds: int
      description: str
      custom_sound: str | None = None
  ```
* **TYPE UNIONS** вместо Optional: `str | None` предпочтительнее `Optional[str]` (Python 3.10+).
* **TYPED COLLECTIONS:** `list[Task]`, `dict[str, User]` — явно указывай типы элементов.
* **from __future__ import annotations** — для forward references если нужно.

### Explicit Dependencies
* **DEPENDENCY INJECTION ЧЕРЕЗ КОНСТРУКТОР** — всегда видимые зависимости.
* **НИКОГДА:** глобальное состояние, service locators в бизнес-логике, monkey patching.
* **ЯВНЫЕ КОНТРАКТЫ** — Protocol/ABC на границах, конкретные классы внутри.

---

## 📚 ДОКУМЕНТАЦИЯ КАК КОНТЕКСТ (AI-CRITICAL)

### Module-Level Docstrings (опционально, когда нужен контекст)
**ИСПОЛЬЗУЙ КОГДА:**
- Файл содержит неочевидную логику
- Много зависимостей или связей с другими модулями
- Специфичные бизнес-правила
- Сложная ответственность

**НЕ НУЖЕН ДЛЯ:**
- Простых Tab классов с очевидной логикой
- Тривиальных helpers
- Простых Storage классов
- **DOCSTRING ≠ ПОДПИРАЕМ ГИГАНТ** — если файл распухает, режь по правилам из 📁 ОРГАНИЗАЦИЯ ФАЙЛОВ.

**ФОРМАТ (когда используешь):**
```python
"""
Назначение: [Только если неочевидно из названия модуля]
Ключевые особенности: [Что важно знать]
Связи: [Если есть важные зависимости от других модулей]

Example:
    >>> reminder = HabitReminder(habits_tab)
    >>> reminder.start()
"""
```

**Пример — нужен docstring:**
```python
"""
Назначение: Многопоточная система напоминаний о привычках с учетом временных окон
Особенности:
    - Работает в daemon thread с проверкой каждые 10 секунд
    - Учитывает active_hours для каждой привычки
    - Предотвращает дублирование уведомлений через shown_reminders
    - Thread-safe обновление через save_habits() с блокировкой
Связи: HabitsTab, SoundPlayer, ToastNotification

ВАЖНО: Вызывай save_habits() только с приобретенным self._save_lock!
"""
class HabitReminder:
    ...
```

**Пример — docstring НЕ нужен:**
```python
# Просто начинай с кода:
class TodoListTab(ttk.Frame):
    ...

class TimerConfig:
    ...
```

### Function Docstrings — КОГДА НУЖЕН

**ОБЯЗАТЕЛЕН ТОЛЬКО КОГДА:**
1. **Типы не очевидны** — сложные структуры данных, nested dicts
2. **Бизнес-логика не очевидна** — специальные правила, edge cases
3. **Side effects** — изменение состояния, файловые операции
4. **Thread safety concerns** — требования к блокировкам, race conditions

**НЕ НУЖЕН КОГДА:**
- Type hints уже всё объясняют
- Название метода говорит само за себя
- Параметры очевидны из типов и имён

**ПРИМЕРЫ:**

✅ **ХОРОШО — добавляет ценность:**
```python
def save_habits(self) -> None:
    """
    Сохраняет состояние привычек в habits.json.

    Thread-safe: использует self._save_lock для защиты от race conditions
    с HabitReminder daemon thread.

    Raises:
        OSError: Если не удалось записать файл
    """
    with self._save_lock:
        # ...

def load_timers(self) -> list[TimerConfig]:
    """
    Загружает конфигурацию таймеров из timers.json.

    Returns:
        Список конфигураций таймеров. Пустой список если файл не существует
        или содержит невалидный JSON.

    Note:
        Использует silent fallback - ошибки не показываются пользователю,
        просто возвращается пустой список.
    """
    try:
        # ...
```

❌ **ПЛОХО — дублирует очевидное:**
```python
def get_user(self, user_id: int) -> User:
    """
    Получить пользователя

    Args:
        user_id: ID пользователя

    Returns:
        User: Объект пользователя
    """
    # Type hints уже всё говорят!

def start_timer(self) -> None:
    """Запустить таймер"""
    # Очевидно из названия!
```

✅ **ХОРОШО — минимально когда типы достаточны:**
```python
def get_user(self, user_id: int) -> User:
    # Понятно без docstring
    ...

def is_running(self) -> bool:
    # Очевидно из типов
    ...
```

**GOLDEN RULE: Docstring объясняет "ЧТО и ПОЧЕМУ", не "КАК". Если type hints и название уже ответили — docstring не нужен.**

### Project Documentation
**КРИТИЧНЫЕ ФАЙЛЫ (в корне или `/docs`):**
* **`README.md`** — как запустить, основные команды, структура (НА РУССКОМ)
* **`AI_README.md`** — полная архитектура для AI-ассистентов (НА РУССКОМ)
* **`docs/ai/contract.md`** — этот файл, политика разработки (НА РУССКОМ)

**AI читает эти файлы для контекста** — держи актуальными.

---

## 🧠 ФИЛОСОФИЯ (ОСНОВЫ)

* **ПРОСТЫЕ НАРУЖНЫЕ ИНТЕРФЕЙСЫ, СЛОЖНОСТЬ — ВНУТРИ** (ОБЩИЙ ПРИНЦИП УПРОЩЕНИЯ ИНТЕРФЕЙСОВ).
* **DRY ПО ПРАВИЛУ ТРЕТЬЕГО РАЗА** (ОБЩИЕ АБСТРАКЦИИ — ТОЛЬКО ПО ФАКТУ ПОВТОРОВ).
* **FUNCTIONAL CORE / IMPERATIVE SHELL**: ЛОГИКА — ЧИСТАЯ; ЭФФЕКТЫ — НА ПЕРИФЕРИИ.
* **SOLID ТОЛЬКО ЕСЛИ УПРОЩАЕТ**, НЕ РАДИ ПАТТЕРНА.
* **CODE FOR HUMANS AND AI** — читаемость кода = понятность для AI = лучшая генерация.

---

## 🧩 ПАТТЕРНЫ (ТОЛЬКО КОГДА УПРОЩАЮТ)

* **FACTORY** — СЛОЖНОЕ СОЗДАНИЕ.
* **STRATEGY/STATE** — КОГДА ВЕТОК > 2.
* **OBSERVER** — СОБЫТИЯ И ПОДПИСЧИКИ (для Tkinter callbacks).
* **STORAGE/REPOSITORY** — ИЗОЛЯЦИЯ DATA ACCESS, НЕ ДЛЯ КАЖДОЙ МОДЕЛИ.
* **DTO/DATACLASS** — ДЛЯ СТРУКТУР ДАННЫХ, ЧЁТКИЕ КОНТРАКТЫ.
* **НЕ ДЕЛАТЬ "АРХИТЕКТУРНЫЙ ТЕАТР"** — паттерн должен решать реальную проблему.

---

## 📂 СТРУКТУРА ПРОЕКТА И ОРГАНИЗАЦИЯ

### Feature-Based Organization (Текущая структура)
```
src/
  components/        # Переиспользуемые UI компоненты
    timer.py
  tabs/              # Вкладки приложения (фичи)
    habits_tab.py
    medication_tab.py
    todo_list_tab.py
    telegram_tab.py
    pushup_tracker_tab.py
  windows/           # Окна приложения
    main_window.py
    main_timer_window.py
  utils/             # Утилиты и хелперы
    constants.py
    resource_path.py
    sound_utils.py
    habit_reminder.py
    timer_notification.py
  resources/         # Ресурсы (изображения, звуки)
    images/
    sounds/
  main.py           # Точка входа
```

**ПРЕИМУЩЕСТВА:** изоляция контекста, легче работать AI с одной фичей, clear boundaries.

### File Naming Consistency
* Tabs: `habits_tab.py`, `todo_list_tab.py`
* Windows: `main_window.py`, `main_timer_window.py`
* Utils: `sound_utils.py`, `resource_path.py`
* Components: `timer.py`
* Classes: `HabitsTab`, `MainWindow`, `Timer` (PascalCase)

---

## ⚙️ TKINTER + TTKTHEMES

### Best Practices
* **НАСЛЕДУЙ ttk.Frame** для всех tab классов
* **ИСПОЛЬЗУЙ ttk WIDGETS** вместо tk (современный вид, темы)
* **РАЗДЕЛЯЙ setup_ui() И load_data()** — отдельные методы
* **GRID ИЛИ PACK, НЕ СМЕШИВАЙ** в одном контейнере
* **ЯВНЫЕ СТИЛИ** через ttk.Style для кастомизации

### Tkinter + AI Specifics
* **ЯВНО УКАЗЫВАЙ "Tkinter + ttkthemes"** в промптах
* **TclError PROTECTION** — всегда wrap UI updates в try/except tk.TclError
* **THREADING** — UI updates только в main thread, используй after() для delayed updates
* **DESTROY CHECKS** — проверяй winfo_exists() перед обновлением виджетов

### Threading в Tkinter
```python
# ✅ ПРАВИЛЬНО - UI update в main thread
def update_from_thread(self):
    try:
        if self.label and self.label.winfo_exists():
            self.label.configure(text="Updated")
    except tk.TclError:
        pass  # Widget destroyed

# ✅ ПРАВИЛЬНО - scheduled update
self.after(100, self.update_display)

# ❌ НЕПРАВИЛЬНО - direct update из другого потока
threading.Thread(target=lambda: self.label.configure(text="Bad"))
```

---

## 📦 ДАННЫЕ И PERSISTENCE

### JSON Storage (текущий подход)
* **ВСЕ ИЗМЕНЕНИЯ — НЕМЕДЛЕННАЯ ЗАПИСЬ** (текущее поведение, но можно оптимизировать)
* **UTF-8 ENCODING ОБЯЗАТЕЛЕН**: `encoding="utf-8"`
* **ensure_ascii=False** для сохранения русского текста
* **indent=2** для читаемости

### Thread Safety (КРИТИЧНО)
* **ИСПОЛЬЗУЙ threading.Lock** для защиты save/load операций
* **DAEMON THREADS** не должны писать без блокировок
* **ПРОВЕРЯЙ RACE CONDITIONS** — HabitReminder vs UI updates

### Atomic Writes Pattern
```python
import os
import json
import tempfile

def save_data_atomic(self, data: dict, filepath: str) -> None:
    """Атомарная запись через temp file + rename"""
    temp_fd, temp_path = tempfile.mkstemp(dir=os.path.dirname(filepath))
    try:
        with os.fdopen(temp_fd, 'w', encoding='utf-8') as f:
            json.dump(data, f, ensure_ascii=False, indent=2)
        os.replace(temp_path, filepath)  # Atomic on POSIX
    except:
        os.unlink(temp_path)
        raise
```

---

## 🧰 PYGAME (ЗВУКИ)

### Sound Playback Best Practices
* **ИНИЦИАЛИЗИРУЙ mixer ОДИН РАЗ** при старте приложения
* **ПРОВЕРЯЙ СУЩЕСТВОВАНИЕ ФАЙЛОВ** перед загрузкой
* **FALLBACK НА WINSOUND** (Windows) если pygame недоступен
* **GRACEFUL DEGRADATION** если нет звуковой карты

```python
from pygame import mixer
import platform

if platform.system() == "Windows":
    import winsound

class SoundPlayer:
    def __init__(self):
        try:
            mixer.init()
            self.available = True
        except:
            self.available = False

    def play(self, sound_path: str) -> None:
        if not self.available:
            return

        try:
            mixer.music.load(sound_path)
            mixer.music.play()
        except Exception:
            # Fallback для Windows
            if platform.system() == "Windows":
                winsound.PlaySound(sound_path, winsound.SND_FILENAME | winsound.SND_ASYNC)
```

---

## 🔌 ПАТТЕРНЫ ПРОЕКТИРОВАНИЯ

### Abstract Base Classes (границы)
```python
from abc import ABC, abstractmethod

class Storage(ABC):
    @abstractmethod
    def save(self, data: dict) -> None:
        ...

    @abstractmethod
    def load(self) -> dict:
        ...
```

**ИСПОЛЬЗУЙ НА ГРАНИЦАХ:**
- Storage interfaces
- Notification systems
- Sound players
- External APIs (Telegram)

**НЕ ИСПОЛЬЗУЙ ВНУТРИ** для простых случаев.

---

## 📝 ЛОГИ И ОШИБКИ

### Error Handling Patterns
**Pattern 1: Silent Fallback** (для load операций)
```python
def load_data(self) -> dict:
    try:
        with open("data.json", "r", encoding="utf-8") as f:
            return json.load(f)
    except FileNotFoundError:
        return {}  # Default
    except json.JSONDecodeError as e:
        messagebox.showwarning("Предупреждение", f"Файл поврежден: {e}")
        return {}
```

**Pattern 2: User Notification** (для save операций)
```python
def save_data(self, data: dict) -> None:
    try:
        with open("data.json", "w", encoding="utf-8") as f:
            json.dump(data, f, ensure_ascii=False, indent=2)
    except Exception as e:
        messagebox.showerror("Ошибка", f"Не удалось сохранить: {e}")
```

**Pattern 3: TclError Protection** (для UI updates)
```python
try:
    self.label.configure(text="New text")
except tk.TclError:
    pass  # Widget was destroyed
```

### Logging (РЕКОМЕНДУЕТСЯ ДОБАВИТЬ)
```python
import logging

logging.basicConfig(
    filename='app.log',
    level=logging.INFO,
    format='%(asctime)s - %(name)s - %(levelname)s - %(message)s',
    encoding='utf-8'
)

logger = logging.getLogger(__name__)
logger.info("Habit reminder triggered", extra={"habit": habit_name})
```

---

## 🤖 AI-ASSISTED DEVELOPMENT

### General Principles
* **AI = JUNIOR DEVELOPER** — требует oversight, code review, testing.
* **HUMANS ACCOUNTABLE** — за весь committed код отвечают люди, не AI.
* **TEST EVERYTHING** — AI-generated код требует тщательной проверки.
* **CONTEXT IS KING** — хороший контекст важнее clever промптов.

### Prompting Best Practices
**STRUCTURED PROMPTS:**
```
Context: Python 3.x, Tkinter + ttkthemes, PyGame, thread-safe JSON storage
Task: Добавить поддержку групп привычек с фильтрацией в UI
Constraints: Сохранять backward compatibility с habits.json, thread-safe save
Standards: Type hints везде, docstrings для неочевидного, guard clauses
Output: Обновленный HabitsTab, миграция данных, unit tests
Язык: UI на русском, код на английском
```

**SPECIFICITY WINS:**
- ✅ "Создай Storage класс с atomic writes, thread-safe, JSON backend, fallback на defaults"
- ❌ "Создай Storage"

### Code Review Checklist (для AI-generated кода)
- [ ] Все type hints полные и корректные
- [ ] Docstrings НЕ дублируют type hints (только где добавляют ценность)
- [ ] Guard clauses использованы
- [ ] Вложенность ≤ 2 уровня
- [ ] Функции ≤ 50 строк
- [ ] UI тексты на русском
- [ ] Docstrings/комментарии на русском
- [ ] Имена переменных/функций на английском
- [ ] UTF-8 encoding в file operations
- [ ] Thread safety где нужно (locks для shared state)
- [ ] TclError protection для UI updates
- [ ] Platform checks для OS-specific кода
- [ ] Resource paths через get_resource_path()

---

## 🚫 ЖЁСТКИЕ ЗАПРЕТЫ

### Code Structure
* **НАРУШЕНИЕ ПРИОРИТЕТА: KISS > YAGNI > DRY > SOLID**.
* **ИЗБЫТОЧНЫЕ КОММЕНТАРИИ** — не дублируй type hints, не объясняй очевидное.
* **ВЛОЖЕННОСТЬ > 2**, **ФУНКЦИИ > 50 СТРОК**.
* **ЦЕПОЧКИ IF/ELSE ПРИ > 2 ВЕТКАХ**.
* **БУЛЕВЫЕ ФЛАГИ-ПЕРЕКЛЮЧАТЕЛИ ПОВЕДЕНИЯ**.
* **АБСТРАКЦИИ «ВПРОК»**; **АРХИТЕКТУРНЫЙ ТЕАТР**.
* **HARDCODED PATHS** — всегда через get_resource_path() или constants.
* **UI ТЕКСТЫ НА АНГЛИЙСКОМ** — только русский язык!
* **MIXED LANGUAGE** в документации — либо русский, либо английский, не смешивать.

### Tkinter Anti-Patterns
* **UI UPDATES ИЗ ДРУГИХ ПОТОКОВ** без after() или queue
* **MIXING PACK AND GRID** в одном контейнере
* **GLOBAL STATE** для widget references
* **ЗАБЫВАТЬ destroy()** при закрытии окон/виджетов
* **NO TclError PROTECTION** в threaded code

### AI-Specific Anti-Patterns
* **BLIND COPY-PASTE** AI suggestions
* **VAGUE PROMPTS** без контекста проекта
* **INSUFFICIENT CONTEXT** — не предоставлять AI_README.md и contract.md
* **IGNORING LANGUAGE REQUIREMENTS** — забывать про русский UI
* **NO PLATFORM CHECKS** — генерить код без учета Windows/Linux различий

### Git
* **`git add` И `git commit` НИКОГДА БЕЗ ЯВНОГО ЗАПРОСА**: СОЗДАВАЙ/РЕДАКТИРУЙ ФАЙЛЫ ТОЛЬКО; КОММИТЫ — ТОЛЬКО НА ЯВНЫЙ ЗАПРОС РАЗРАБОТЧИКА.

---

## 🔎 МЕТОДОЛОГИИ ДЛЯ AI (ВНУТРЕННЕ)

* **КОНСТИТУЦИОННЫЙ КОНТРОЛЬ:** ПОВЕДЕНИЕ СООТВЕТСТВУЕТ ЯВНЫМ ПРИНЦИПАМ (HELPFUL/HONEST/HARMLESS).
* **REACT-ПОДХОД:** РАЗДЕЛЯТЬ «ДУМАТЬ» И «ДЕЙСТВОВАТЬ»; ПОДТВЕРЖДАТЬ ШАГИ.
* **ПРОМПТ-ДИСЦИПЛИНА:** ЯСНОСТЬ, КОНКРЕТИКА, ЯВНЫЕ КРИТЕРИИ УСПЕХА.
* **PLANNING → IMPLEMENTATION → VALIDATION** — разделяй фазы.

---

## ✅ КОНТРОЛЬ СОБЛЮДЕНИЯ

### Manual Review Focus (для AI-generated)
* **LOGIC CORRECTNESS** — проверяй бизнес-логику
* **LANGUAGE COMPLIANCE** — UI на русском, код на английском
* **THREAD SAFETY** — locks где нужно
* **PLATFORM COMPATIBILITY** — Windows + Linux + WSL
* **RESOURCE PATHS** — через get_resource_path()
* **ENCODING** — UTF-8 везде
* **ERROR HANDLING** — правильные patterns

---

## 📖 USAGE GUIDE ДЛЯ AI-АГЕНТОВ

### При генерации кода ВСЕГДА:
1. Читай contract.md и AI_README.md
2. Проверяй tech stack (Python 3.x, Tkinter, PyGame, Telethon)
3. Добавляй type hints везде
4. **UI тексты ТОЛЬКО на русском**
5. **Docstrings/комментарии на русском**
6. **Код (переменные/функции) на английском**
7. Используй guard clauses
8. Держи функции ≤ 50 строк, вложенность ≤ 2
9. Проверяй platform-specific код (Windows vs Linux)
10. Используй get_resource_path() для ресурсов

**ЗАПРЕЩЕНО:**
- UI тексты на английском
- Docstrings дублирующие type hints
- Прямые пути к ресурсам (не через get_resource_path)
- UI updates из потоков без защиты
- Отсутствие encoding="utf-8" в file operations

### При рефакторинге ВСЕГДА:
1. Сохраняй backward compatibility с JSON файлами
2. Не меняй UI language (русский)
3. Проверяй thread safety
4. Тестируй на Windows и Linux если возможно
5. Обновляй docstrings если логика изменилась

### Platform-Specific Checklist
- [ ] `platform.system()` checks для OS-specific кода
- [ ] WSL detection для tray icon
- [ ] Windows: winsound fallback для звуков
- [ ] Linux: pygame.mixer для звуков
- [ ] PyInstaller: sys._MEIPASS handling
- [ ] Path normalization (slashes)

---

## 🎓 ДОПОЛНИТЕЛЬНЫЕ РЕСУРСЫ

### Python Best Practices
* **PEP 8** — Style Guide for Python Code
* **PEP 257** — Docstring Conventions
* **Type Hints** — PEP 484, 585, 604

### Tkinter Resources
* **effbot.org/tkinterbook** — comprehensive Tkinter guide
* **TkDocs** — modern Tkinter documentation

### Project-Specific
* **AI_README.md** — полная архитектура проекта
* **README.md** — user-facing документация
* **app.spec** — PyInstaller configuration

---

## 🌐 LANGUAGE POLICY (КРИТИЧНО)

### Русский язык для UI
**ОБЯЗАТЕЛЬНО:**
- Все кнопки, метки, заголовки
- Сообщения об ошибках (messagebox)
- Уведомления (toast, tray)
- Названия вкладок
- Placeholder текст
- Tooltips

**ПРИМЕРЫ:**
```python
# ✅ ПРАВИЛЬНО
ttk.Button(self, text="Добавить таймер", command=self.add_timer)
messagebox.showerror("Ошибка", "Не удалось сохранить файл")
self.notebook.add(timer_tab, text="Таймер")

# ❌ НЕПРАВИЛЬНО
ttk.Button(self, text="Add Timer", command=self.add_timer)
messagebox.showerror("Error", "Failed to save file")
self.notebook.add(timer_tab, text="Timer")
```

### Русский язык для документации
**ОБЯЗАТЕЛЬНО:**
- Module docstrings
- Function docstrings
- Inline комментарии (если используются)
- README файлы
- TODO комментарии

**ПРИМЕРЫ:**
```python
# ✅ ПРАВИЛЬНО
def save_habits(self) -> None:
    """
    Сохраняет привычки в habits.json с защитой от race conditions.

    Raises:
        OSError: Если не удалось записать файл
    """

# ❌ НЕПРАВИЛЬНО
def save_habits(self) -> None:
    """
    Saves habits to habits.json with race condition protection.

    Raises:
        OSError: If file write fails
    """
```

### Английский язык для кода
**ОБЯЗАТЕЛЬНО:**
- Имена переменных
- Имена функций
- Имена классов
- Имена модулей
- Константы

**ПРИМЕРЫ:**
```python
# ✅ ПРАВИЛЬНО
class HabitsTab(ttk.Frame):
    def __init__(self, parent):
        self.habits = {}
        self.custom_times = []

    def save_habits(self) -> None:
        ...

# ❌ НЕПРАВИЛЬНО
class ВкладкаПривычек(ttk.Frame):
    def __init__(self, родитель):
        self.привычки = {}
        self.кастомные_времена = []

    def сохранить_привычки(self) -> None:
        ...
```

---

**VERSION HISTORY:**
- v1 (2025-11-15): Первая версия политики для Smart Multi-Timer, адаптация под Python/Tkinter стек

---

**КОНТАКТ:**
Telegram: [@teagamesen](https://t.me/teagamesen)
